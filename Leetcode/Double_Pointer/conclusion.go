package conclusion

func conclusion() {

	// 一般的关键字：连续数组，最少次数，定长度为..或满足某条件（和不超过...）
	// 写法：
	// 1.定义 l,r 双指针

	/*

		2. 定义保存每轮结果的变量count
		count := 0
		3. 定义返回结果result
		result := maxint || minint || 0

		for r < n {
			// 根据下标 r 进行操作

			// 判断窗口是否满足条件 r-l+1 > k ?
			// 若超过窗口大小
			// 移动左指针
			// 移除左指针下标所指元素

			if r - l + 1 > k {
				count -= || --
				l ++
			}

			// 如果满足窗口条件，更新结果

			if r - l == k ... {
				result = ...
			}

			// 更新右指针
			r++
		}
			return result

	*/

}
